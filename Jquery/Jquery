jq对象 -> js原生对象 $obj.get(0) 或者 $obj[0]
js原生对象 -> jq对象 $(obj)

$(document).ready(function())    文档就绪函数
    可以写多个 快于window.onload();(对于JS代码写在head中 onload只能写一个 后面的onload会覆盖之前的onload方法)
    文档就绪：$(function(){
        ...
    })